var express = require('express');
var router = express.Router();
var pass = require('../config/pass');

//router.get('/user/lookup', pass.ensureAuthenticated, pass.ensureAdmin, user.lookup);

module.exports = router;